name: PHP forever
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  NT4-is-the-best-ever:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
        
      - name: Repodagi fayllar royxati
        run: |
          ls ${{ github.workspace }}
      - run: |
          pwd
